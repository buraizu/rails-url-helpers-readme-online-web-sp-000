<% @posts.each do |post| %>
  <div><%= link_to post.title, "/posts/#{post.id}" %></div>
  <!-- <div><a href='<%= "/posts/#{post.id}" %>'><%= post.title %></a></div> -->   <!-- Longer style -->
<% end %>
